# Copy this file to scripts/.env and fill in the real values.
# scripts/.env is gitignored — it will never be committed.
# These mirror the GitHub Actions secrets of the same name.

CANOE_API_URL=https://exchange-gateway.ringpartner.com
CANOE_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
